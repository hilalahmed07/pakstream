// Feature flags — set to false to hide a feature everywhere in the app

// Set isPatchVisible = false to completely hide Patch Management from:
//   admin sidebar, user home page, navbar, analytics, downloads, and all routes.
export const isPatchVisible = false;
